# Kiko Gás Web 💻

Plataforma Web oficial dos serviços Kiko Gás para revendedores.

### Instalação das dependências

Na raiz do projeto execute o seguinte comando:

```sh
npm install # ou npm i
```

Esse comando irá instalar as dependências necessárias para que o projeto possa ser executado com sucesso.

### Como executar?

Ainda na raiz do projeto execute o seguinte comando:

```sh
npm run dev
```

Esse comando irá iniciar o painel web em modo de desenvolvedor. No terminal, ficará disponível uma URL para que você possa acessar o painel em seu navegador.
